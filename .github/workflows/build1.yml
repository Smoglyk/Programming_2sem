name: build

on: push

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Checkout code
        
        run: |
          cd lab1/
          Checkout code main1_test
          ./main1_test
